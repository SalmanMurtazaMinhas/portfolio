import React from 'react'
import { motion } from 'framer-motion'

export default function Projects() {
  return (
    <motion.div
      className='pro-div-con'
    >
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
      
      <motion.div className='card'
        initial={{ opacity: 0 }}
        whileInView={{ opacity: 1}}
        viewport={{ once: true }}
      >o</motion.div>
    </motion.div>
  )
}
